# Reference Worker infrastructure — pure computation sandbox.
# Tests the WASI shim and HTTP handler using whichever plugin wasm is prepared.
# NO bindings allowed: no R2, D1, KV, secrets. WASM is statically imported.
name = "reference-test"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
