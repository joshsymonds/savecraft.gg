[package]
name = "clausewitz-core"
version = "0.1.0"
edition = "2024"

[dependencies]
jomini = "0.30"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
