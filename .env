# Shared non-secret defaults. Checked into git.
# Local overrides and secrets go in .env.local (gitignored).

# Clerk publishable key (safe to expose — it's public)
# Set in .env.local: PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
