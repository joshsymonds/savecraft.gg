// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: savecraft/v1/protocol.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../google/protobuf/struct";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "savecraft.v1";

export enum ParseErrorType {
  PARSE_ERROR_TYPE_UNSPECIFIED = 0,
  PARSE_ERROR_TYPE_UNSUPPORTED_VERSION = 1,
  PARSE_ERROR_TYPE_CORRUPT_FILE = 2,
  PARSE_ERROR_TYPE_PARSE_ERROR = 3,
  UNRECOGNIZED = -1,
}

export function parseErrorTypeFromJSON(object: any): ParseErrorType {
  switch (object) {
    case 0:
    case "PARSE_ERROR_TYPE_UNSPECIFIED":
      return ParseErrorType.PARSE_ERROR_TYPE_UNSPECIFIED;
    case 1:
    case "PARSE_ERROR_TYPE_UNSUPPORTED_VERSION":
      return ParseErrorType.PARSE_ERROR_TYPE_UNSUPPORTED_VERSION;
    case 2:
    case "PARSE_ERROR_TYPE_CORRUPT_FILE":
      return ParseErrorType.PARSE_ERROR_TYPE_CORRUPT_FILE;
    case 3:
    case "PARSE_ERROR_TYPE_PARSE_ERROR":
      return ParseErrorType.PARSE_ERROR_TYPE_PARSE_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ParseErrorType.UNRECOGNIZED;
  }
}

export function parseErrorTypeToJSON(object: ParseErrorType): string {
  switch (object) {
    case ParseErrorType.PARSE_ERROR_TYPE_UNSPECIFIED:
      return "PARSE_ERROR_TYPE_UNSPECIFIED";
    case ParseErrorType.PARSE_ERROR_TYPE_UNSUPPORTED_VERSION:
      return "PARSE_ERROR_TYPE_UNSUPPORTED_VERSION";
    case ParseErrorType.PARSE_ERROR_TYPE_CORRUPT_FILE:
      return "PARSE_ERROR_TYPE_CORRUPT_FILE";
    case ParseErrorType.PARSE_ERROR_TYPE_PARSE_ERROR:
      return "PARSE_ERROR_TYPE_PARSE_ERROR";
    case ParseErrorType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum GameStatusEnum {
  GAME_STATUS_ENUM_UNSPECIFIED = 0,
  GAME_STATUS_ENUM_DETECTED = 1,
  GAME_STATUS_ENUM_WATCHING = 2,
  GAME_STATUS_ENUM_ERROR = 3,
  GAME_STATUS_ENUM_NOT_FOUND = 4,
  UNRECOGNIZED = -1,
}

export function gameStatusEnumFromJSON(object: any): GameStatusEnum {
  switch (object) {
    case 0:
    case "GAME_STATUS_ENUM_UNSPECIFIED":
      return GameStatusEnum.GAME_STATUS_ENUM_UNSPECIFIED;
    case 1:
    case "GAME_STATUS_ENUM_DETECTED":
      return GameStatusEnum.GAME_STATUS_ENUM_DETECTED;
    case 2:
    case "GAME_STATUS_ENUM_WATCHING":
      return GameStatusEnum.GAME_STATUS_ENUM_WATCHING;
    case 3:
    case "GAME_STATUS_ENUM_ERROR":
      return GameStatusEnum.GAME_STATUS_ENUM_ERROR;
    case 4:
    case "GAME_STATUS_ENUM_NOT_FOUND":
      return GameStatusEnum.GAME_STATUS_ENUM_NOT_FOUND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GameStatusEnum.UNRECOGNIZED;
  }
}

export function gameStatusEnumToJSON(object: GameStatusEnum): string {
  switch (object) {
    case GameStatusEnum.GAME_STATUS_ENUM_UNSPECIFIED:
      return "GAME_STATUS_ENUM_UNSPECIFIED";
    case GameStatusEnum.GAME_STATUS_ENUM_DETECTED:
      return "GAME_STATUS_ENUM_DETECTED";
    case GameStatusEnum.GAME_STATUS_ENUM_WATCHING:
      return "GAME_STATUS_ENUM_WATCHING";
    case GameStatusEnum.GAME_STATUS_ENUM_ERROR:
      return "GAME_STATUS_ENUM_ERROR";
    case GameStatusEnum.GAME_STATUS_ENUM_NOT_FOUND:
      return "GAME_STATUS_ENUM_NOT_FOUND";
    case GameStatusEnum.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Message is the top-level WebSocket envelope.
 * All messages on the daemon↔server↔UI channel use this wrapper.
 * The oneof field numbers are grouped by category with gaps for future additions.
 */
export interface Message {
  payload?:
    | //
    /** Daemon lifecycle (1-9) */
    { $case: "daemonOnline"; daemonOnline: DaemonOnline }
    | { $case: "daemonOffline"; daemonOffline: DaemonOffline }
    | //
    /** Game discovery (10-19) */
    { $case: "scanStarted"; scanStarted: ScanStarted }
    | { $case: "scanCompleted"; scanCompleted: ScanCompleted }
    | { $case: "gameDetected"; gameDetected: GameDetected }
    | { $case: "gameNotFound"; gameNotFound: GameNotFound }
    | { $case: "watching"; watching: Watching }
    | { $case: "gamesDiscovered"; gamesDiscovered: GamesDiscovered }
    | //
    /** Parse lifecycle (20-29) */
    { $case: "parseStarted"; parseStarted: ParseStarted }
    | { $case: "pluginStatus"; pluginStatus: PluginStatus }
    | { $case: "parseCompleted"; parseCompleted: ParseCompleted }
    | { $case: "parseFailed"; parseFailed: ParseFailed }
    | //
    /** Push lifecycle (30-39) */
    { $case: "pushStarted"; pushStarted: PushStarted }
    | { $case: "pushCompleted"; pushCompleted: PushCompleted }
    | { $case: "pushFailed"; pushFailed: PushFailed }
    | //
    /** Plugin management (40-49) */
    { $case: "pluginUpdated"; pluginUpdated: PluginUpdated }
    | //
    /** Commands: server → daemon (50-59) */
    { $case: "configUpdate"; configUpdate: ConfigUpdate }
    | { $case: "rescanGame"; rescanGame: RescanGame }
    | { $case: "pluginAvailable"; pluginAvailable: PluginAvailable }
    | { $case: "discoverGames"; discoverGames: DiscoverGames }
    | //
    /** State: server → UI (60-69) */
    { $case: "deviceState"; deviceState: DeviceState }
    | //
    /** User actions: UI → server → daemon (70-79) */
    { $case: "testPath"; testPath: TestPath }
    | { $case: "testPathResult"; testPathResult: TestPathResult }
    | undefined;
}

/** Daemon process started and connected to the server. */
export interface DaemonOnline {
  deviceId: string;
  version: string;
  timestamp: Date | undefined;
}

/** Daemon shutting down gracefully (SIGTERM). */
export interface DaemonOffline {
  deviceId: string;
  timestamp: Date | undefined;
}

/** Daemon is scanning a directory for save files. */
export interface ScanStarted {
  gameId: string;
  path: string;
}

/** Scan finished. Reports what was found. */
export interface ScanCompleted {
  gameId: string;
  path: string;
  filesFound: number;
  fileNames: string[];
}

/** A game's save directory exists and contains saves. */
export interface GameDetected {
  gameId: string;
  path: string;
  saveCount: number;
}

/** None of the expected save paths exist for this game. */
export interface GameNotFound {
  gameId: string;
  pathsChecked: string[];
}

/** Daemon is actively watching a directory for changes. */
export interface Watching {
  gameId: string;
  path: string;
  filesMonitored: number;
}

/** Results of daemon auto-discovery of save directories. */
export interface GamesDiscovered {
  games: DiscoveredGame[];
}

export interface DiscoveredGame {
  gameId: string;
  name: string;
  path: string;
  fileCount: number;
}

/** Daemon detected a file change and is feeding it to the WASM plugin. */
export interface ParseStarted {
  gameId: string;
  fileName: string;
}

/**
 * Forwarded from plugin stdout status lines during parsing.
 * Plugin authors emit these for long-running or multi-step parses.
 */
export interface PluginStatus {
  gameId: string;
  fileName: string;
  message: string;
}

/** Plugin finished successfully. Identity and summary extracted from plugin output. */
export interface ParseCompleted {
  gameId: string;
  fileName: string;
  identity: SaveIdentity | undefined;
  summary: string;
  sectionsCount: number;
  sizeBytes: number;
}

/** Plugin failed to parse the file. */
export interface ParseFailed {
  gameId: string;
  fileName: string;
  errorType: ParseErrorType;
  message: string;
}

/** Daemon is uploading parsed game state to the server via HTTP POST. */
export interface PushStarted {
  gameId: string;
  summary: string;
  sizeBytes: number;
}

/** Upload succeeded. Server assigned/resolved the save UUID. */
export interface PushCompleted {
  gameId: string;
  saveUuid: string;
  summary: string;
  snapshotSizeBytes: number;
  durationMs: number;
}

/** Upload failed. will_retry indicates if the daemon will retry automatically. */
export interface PushFailed {
  gameId: string;
  message: string;
  willRetry: boolean;
}

/** Daemon successfully updated a plugin to a new version. */
export interface PluginUpdated {
  gameId: string;
  version: string;
}

/** Full config push. Sent when user changes settings in the web UI. */
export interface ConfigUpdate {
  games: { [key: string]: GameConfig };
}

export interface ConfigUpdate_GamesEntry {
  key: string;
  value: GameConfig | undefined;
}

export interface GameConfig {
  savePath: string;
  enabled: boolean;
  fileExtensions: string[];
}

/** Request the daemon to rescan a game's save directory. */
export interface RescanGame {
  gameId: string;
}

/** Notify daemon that a plugin update is available for download. */
export interface PluginAvailable {
  gameId: string;
  version: string;
  url: string;
}

/** Request the daemon to re-run save directory discovery. */
export interface DiscoverGames {
}

/**
 * Full device state snapshot. Sent to UI on WebSocket connect for cold start.
 * Constructed from D1 persisted events so the page isn't blank.
 */
export interface DeviceState {
  devices: DeviceInfo[];
}

export interface DeviceInfo {
  deviceId: string;
  online: boolean;
  lastSeen: Date | undefined;
  games: GameInfo[];
}

export interface GameInfo {
  gameId: string;
  gameName: string;
  status: GameStatusEnum;
  saves: SaveInfo[];
  lastActivity: Date | undefined;
}

export interface SaveInfo {
  saveUuid: string;
  identity: SaveIdentity | undefined;
  summary: string;
  lastUpdated: Date | undefined;
}

/** User entered a custom save path in the web UI. Test if it's valid. */
export interface TestPath {
  gameId: string;
  path: string;
}

/** Result of a TestPath request. */
export interface TestPathResult {
  gameId: string;
  path: string;
  valid: boolean;
  filesFound: number;
  fileNames: string[];
}

/**
 * Compact save identity for display in status events and UI.
 * Derived from the full GameState identity emitted by the plugin.
 */
export interface SaveIdentity {
  name: string;
  /**
   * Game-specific display metadata. e.g. {"class": "Paladin", "level": 89}
   * Uses Struct to preserve value types (numbers, strings, bools).
   */
  extra: { [key: string]: any } | undefined;
}

function createBaseMessage(): Message {
  return { payload: undefined };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "daemonOnline":
        DaemonOnline.encode(message.payload.daemonOnline, writer.uint32(10).fork()).join();
        break;
      case "daemonOffline":
        DaemonOffline.encode(message.payload.daemonOffline, writer.uint32(18).fork()).join();
        break;
      case "scanStarted":
        ScanStarted.encode(message.payload.scanStarted, writer.uint32(82).fork()).join();
        break;
      case "scanCompleted":
        ScanCompleted.encode(message.payload.scanCompleted, writer.uint32(90).fork()).join();
        break;
      case "gameDetected":
        GameDetected.encode(message.payload.gameDetected, writer.uint32(98).fork()).join();
        break;
      case "gameNotFound":
        GameNotFound.encode(message.payload.gameNotFound, writer.uint32(106).fork()).join();
        break;
      case "watching":
        Watching.encode(message.payload.watching, writer.uint32(114).fork()).join();
        break;
      case "gamesDiscovered":
        GamesDiscovered.encode(message.payload.gamesDiscovered, writer.uint32(122).fork()).join();
        break;
      case "parseStarted":
        ParseStarted.encode(message.payload.parseStarted, writer.uint32(162).fork()).join();
        break;
      case "pluginStatus":
        PluginStatus.encode(message.payload.pluginStatus, writer.uint32(170).fork()).join();
        break;
      case "parseCompleted":
        ParseCompleted.encode(message.payload.parseCompleted, writer.uint32(178).fork()).join();
        break;
      case "parseFailed":
        ParseFailed.encode(message.payload.parseFailed, writer.uint32(186).fork()).join();
        break;
      case "pushStarted":
        PushStarted.encode(message.payload.pushStarted, writer.uint32(242).fork()).join();
        break;
      case "pushCompleted":
        PushCompleted.encode(message.payload.pushCompleted, writer.uint32(250).fork()).join();
        break;
      case "pushFailed":
        PushFailed.encode(message.payload.pushFailed, writer.uint32(258).fork()).join();
        break;
      case "pluginUpdated":
        PluginUpdated.encode(message.payload.pluginUpdated, writer.uint32(322).fork()).join();
        break;
      case "configUpdate":
        ConfigUpdate.encode(message.payload.configUpdate, writer.uint32(402).fork()).join();
        break;
      case "rescanGame":
        RescanGame.encode(message.payload.rescanGame, writer.uint32(410).fork()).join();
        break;
      case "pluginAvailable":
        PluginAvailable.encode(message.payload.pluginAvailable, writer.uint32(418).fork()).join();
        break;
      case "discoverGames":
        DiscoverGames.encode(message.payload.discoverGames, writer.uint32(426).fork()).join();
        break;
      case "deviceState":
        DeviceState.encode(message.payload.deviceState, writer.uint32(482).fork()).join();
        break;
      case "testPath":
        TestPath.encode(message.payload.testPath, writer.uint32(562).fork()).join();
        break;
      case "testPathResult":
        TestPathResult.encode(message.payload.testPathResult, writer.uint32(570).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "daemonOnline", daemonOnline: DaemonOnline.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "daemonOffline", daemonOffline: DaemonOffline.decode(reader, reader.uint32()) };
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = { $case: "scanStarted", scanStarted: ScanStarted.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = { $case: "scanCompleted", scanCompleted: ScanCompleted.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = { $case: "gameDetected", gameDetected: GameDetected.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.payload = { $case: "gameNotFound", gameNotFound: GameNotFound.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.payload = { $case: "watching", watching: Watching.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.payload = {
            $case: "gamesDiscovered",
            gamesDiscovered: GamesDiscovered.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.payload = { $case: "parseStarted", parseStarted: ParseStarted.decode(reader, reader.uint32()) };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.payload = { $case: "pluginStatus", pluginStatus: PluginStatus.decode(reader, reader.uint32()) };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.payload = { $case: "parseCompleted", parseCompleted: ParseCompleted.decode(reader, reader.uint32()) };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.payload = { $case: "parseFailed", parseFailed: ParseFailed.decode(reader, reader.uint32()) };
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          message.payload = { $case: "pushStarted", pushStarted: PushStarted.decode(reader, reader.uint32()) };
          continue;
        }
        case 31: {
          if (tag !== 250) {
            break;
          }

          message.payload = { $case: "pushCompleted", pushCompleted: PushCompleted.decode(reader, reader.uint32()) };
          continue;
        }
        case 32: {
          if (tag !== 258) {
            break;
          }

          message.payload = { $case: "pushFailed", pushFailed: PushFailed.decode(reader, reader.uint32()) };
          continue;
        }
        case 40: {
          if (tag !== 322) {
            break;
          }

          message.payload = { $case: "pluginUpdated", pluginUpdated: PluginUpdated.decode(reader, reader.uint32()) };
          continue;
        }
        case 50: {
          if (tag !== 402) {
            break;
          }

          message.payload = { $case: "configUpdate", configUpdate: ConfigUpdate.decode(reader, reader.uint32()) };
          continue;
        }
        case 51: {
          if (tag !== 410) {
            break;
          }

          message.payload = { $case: "rescanGame", rescanGame: RescanGame.decode(reader, reader.uint32()) };
          continue;
        }
        case 52: {
          if (tag !== 418) {
            break;
          }

          message.payload = {
            $case: "pluginAvailable",
            pluginAvailable: PluginAvailable.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 53: {
          if (tag !== 426) {
            break;
          }

          message.payload = { $case: "discoverGames", discoverGames: DiscoverGames.decode(reader, reader.uint32()) };
          continue;
        }
        case 60: {
          if (tag !== 482) {
            break;
          }

          message.payload = { $case: "deviceState", deviceState: DeviceState.decode(reader, reader.uint32()) };
          continue;
        }
        case 70: {
          if (tag !== 562) {
            break;
          }

          message.payload = { $case: "testPath", testPath: TestPath.decode(reader, reader.uint32()) };
          continue;
        }
        case 71: {
          if (tag !== 570) {
            break;
          }

          message.payload = { $case: "testPathResult", testPathResult: TestPathResult.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return {
      payload: isSet(object.daemonOnline)
        ? { $case: "daemonOnline", daemonOnline: DaemonOnline.fromJSON(object.daemonOnline) }
        : isSet(object.daemon_online)
        ? { $case: "daemonOnline", daemonOnline: DaemonOnline.fromJSON(object.daemon_online) }
        : isSet(object.daemonOffline)
        ? { $case: "daemonOffline", daemonOffline: DaemonOffline.fromJSON(object.daemonOffline) }
        : isSet(object.daemon_offline)
        ? { $case: "daemonOffline", daemonOffline: DaemonOffline.fromJSON(object.daemon_offline) }
        : isSet(object.scanStarted)
        ? { $case: "scanStarted", scanStarted: ScanStarted.fromJSON(object.scanStarted) }
        : isSet(object.scan_started)
        ? { $case: "scanStarted", scanStarted: ScanStarted.fromJSON(object.scan_started) }
        : isSet(object.scanCompleted)
        ? { $case: "scanCompleted", scanCompleted: ScanCompleted.fromJSON(object.scanCompleted) }
        : isSet(object.scan_completed)
        ? { $case: "scanCompleted", scanCompleted: ScanCompleted.fromJSON(object.scan_completed) }
        : isSet(object.gameDetected)
        ? { $case: "gameDetected", gameDetected: GameDetected.fromJSON(object.gameDetected) }
        : isSet(object.game_detected)
        ? { $case: "gameDetected", gameDetected: GameDetected.fromJSON(object.game_detected) }
        : isSet(object.gameNotFound)
        ? { $case: "gameNotFound", gameNotFound: GameNotFound.fromJSON(object.gameNotFound) }
        : isSet(object.game_not_found)
        ? { $case: "gameNotFound", gameNotFound: GameNotFound.fromJSON(object.game_not_found) }
        : isSet(object.watching)
        ? { $case: "watching", watching: Watching.fromJSON(object.watching) }
        : isSet(object.gamesDiscovered)
        ? { $case: "gamesDiscovered", gamesDiscovered: GamesDiscovered.fromJSON(object.gamesDiscovered) }
        : isSet(object.games_discovered)
        ? { $case: "gamesDiscovered", gamesDiscovered: GamesDiscovered.fromJSON(object.games_discovered) }
        : isSet(object.parseStarted)
        ? { $case: "parseStarted", parseStarted: ParseStarted.fromJSON(object.parseStarted) }
        : isSet(object.parse_started)
        ? { $case: "parseStarted", parseStarted: ParseStarted.fromJSON(object.parse_started) }
        : isSet(object.pluginStatus)
        ? { $case: "pluginStatus", pluginStatus: PluginStatus.fromJSON(object.pluginStatus) }
        : isSet(object.plugin_status)
        ? { $case: "pluginStatus", pluginStatus: PluginStatus.fromJSON(object.plugin_status) }
        : isSet(object.parseCompleted)
        ? { $case: "parseCompleted", parseCompleted: ParseCompleted.fromJSON(object.parseCompleted) }
        : isSet(object.parse_completed)
        ? { $case: "parseCompleted", parseCompleted: ParseCompleted.fromJSON(object.parse_completed) }
        : isSet(object.parseFailed)
        ? { $case: "parseFailed", parseFailed: ParseFailed.fromJSON(object.parseFailed) }
        : isSet(object.parse_failed)
        ? { $case: "parseFailed", parseFailed: ParseFailed.fromJSON(object.parse_failed) }
        : isSet(object.pushStarted)
        ? { $case: "pushStarted", pushStarted: PushStarted.fromJSON(object.pushStarted) }
        : isSet(object.push_started)
        ? { $case: "pushStarted", pushStarted: PushStarted.fromJSON(object.push_started) }
        : isSet(object.pushCompleted)
        ? { $case: "pushCompleted", pushCompleted: PushCompleted.fromJSON(object.pushCompleted) }
        : isSet(object.push_completed)
        ? { $case: "pushCompleted", pushCompleted: PushCompleted.fromJSON(object.push_completed) }
        : isSet(object.pushFailed)
        ? { $case: "pushFailed", pushFailed: PushFailed.fromJSON(object.pushFailed) }
        : isSet(object.push_failed)
        ? { $case: "pushFailed", pushFailed: PushFailed.fromJSON(object.push_failed) }
        : isSet(object.pluginUpdated)
        ? { $case: "pluginUpdated", pluginUpdated: PluginUpdated.fromJSON(object.pluginUpdated) }
        : isSet(object.plugin_updated)
        ? { $case: "pluginUpdated", pluginUpdated: PluginUpdated.fromJSON(object.plugin_updated) }
        : isSet(object.configUpdate)
        ? { $case: "configUpdate", configUpdate: ConfigUpdate.fromJSON(object.configUpdate) }
        : isSet(object.config_update)
        ? { $case: "configUpdate", configUpdate: ConfigUpdate.fromJSON(object.config_update) }
        : isSet(object.rescanGame)
        ? { $case: "rescanGame", rescanGame: RescanGame.fromJSON(object.rescanGame) }
        : isSet(object.rescan_game)
        ? { $case: "rescanGame", rescanGame: RescanGame.fromJSON(object.rescan_game) }
        : isSet(object.pluginAvailable)
        ? { $case: "pluginAvailable", pluginAvailable: PluginAvailable.fromJSON(object.pluginAvailable) }
        : isSet(object.plugin_available)
        ? { $case: "pluginAvailable", pluginAvailable: PluginAvailable.fromJSON(object.plugin_available) }
        : isSet(object.discoverGames)
        ? { $case: "discoverGames", discoverGames: DiscoverGames.fromJSON(object.discoverGames) }
        : isSet(object.discover_games)
        ? { $case: "discoverGames", discoverGames: DiscoverGames.fromJSON(object.discover_games) }
        : isSet(object.deviceState)
        ? { $case: "deviceState", deviceState: DeviceState.fromJSON(object.deviceState) }
        : isSet(object.device_state)
        ? { $case: "deviceState", deviceState: DeviceState.fromJSON(object.device_state) }
        : isSet(object.testPath)
        ? { $case: "testPath", testPath: TestPath.fromJSON(object.testPath) }
        : isSet(object.test_path)
        ? { $case: "testPath", testPath: TestPath.fromJSON(object.test_path) }
        : isSet(object.testPathResult)
        ? { $case: "testPathResult", testPathResult: TestPathResult.fromJSON(object.testPathResult) }
        : isSet(object.test_path_result)
        ? { $case: "testPathResult", testPathResult: TestPathResult.fromJSON(object.test_path_result) }
        : undefined,
    };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.payload?.$case === "daemonOnline") {
      obj.daemonOnline = DaemonOnline.toJSON(message.payload.daemonOnline);
    } else if (message.payload?.$case === "daemonOffline") {
      obj.daemonOffline = DaemonOffline.toJSON(message.payload.daemonOffline);
    } else if (message.payload?.$case === "scanStarted") {
      obj.scanStarted = ScanStarted.toJSON(message.payload.scanStarted);
    } else if (message.payload?.$case === "scanCompleted") {
      obj.scanCompleted = ScanCompleted.toJSON(message.payload.scanCompleted);
    } else if (message.payload?.$case === "gameDetected") {
      obj.gameDetected = GameDetected.toJSON(message.payload.gameDetected);
    } else if (message.payload?.$case === "gameNotFound") {
      obj.gameNotFound = GameNotFound.toJSON(message.payload.gameNotFound);
    } else if (message.payload?.$case === "watching") {
      obj.watching = Watching.toJSON(message.payload.watching);
    } else if (message.payload?.$case === "gamesDiscovered") {
      obj.gamesDiscovered = GamesDiscovered.toJSON(message.payload.gamesDiscovered);
    } else if (message.payload?.$case === "parseStarted") {
      obj.parseStarted = ParseStarted.toJSON(message.payload.parseStarted);
    } else if (message.payload?.$case === "pluginStatus") {
      obj.pluginStatus = PluginStatus.toJSON(message.payload.pluginStatus);
    } else if (message.payload?.$case === "parseCompleted") {
      obj.parseCompleted = ParseCompleted.toJSON(message.payload.parseCompleted);
    } else if (message.payload?.$case === "parseFailed") {
      obj.parseFailed = ParseFailed.toJSON(message.payload.parseFailed);
    } else if (message.payload?.$case === "pushStarted") {
      obj.pushStarted = PushStarted.toJSON(message.payload.pushStarted);
    } else if (message.payload?.$case === "pushCompleted") {
      obj.pushCompleted = PushCompleted.toJSON(message.payload.pushCompleted);
    } else if (message.payload?.$case === "pushFailed") {
      obj.pushFailed = PushFailed.toJSON(message.payload.pushFailed);
    } else if (message.payload?.$case === "pluginUpdated") {
      obj.pluginUpdated = PluginUpdated.toJSON(message.payload.pluginUpdated);
    } else if (message.payload?.$case === "configUpdate") {
      obj.configUpdate = ConfigUpdate.toJSON(message.payload.configUpdate);
    } else if (message.payload?.$case === "rescanGame") {
      obj.rescanGame = RescanGame.toJSON(message.payload.rescanGame);
    } else if (message.payload?.$case === "pluginAvailable") {
      obj.pluginAvailable = PluginAvailable.toJSON(message.payload.pluginAvailable);
    } else if (message.payload?.$case === "discoverGames") {
      obj.discoverGames = DiscoverGames.toJSON(message.payload.discoverGames);
    } else if (message.payload?.$case === "deviceState") {
      obj.deviceState = DeviceState.toJSON(message.payload.deviceState);
    } else if (message.payload?.$case === "testPath") {
      obj.testPath = TestPath.toJSON(message.payload.testPath);
    } else if (message.payload?.$case === "testPathResult") {
      obj.testPathResult = TestPathResult.toJSON(message.payload.testPathResult);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message>, I>>(base?: I): Message {
    return Message.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message>, I>>(object: I): Message {
    const message = createBaseMessage();
    switch (object.payload?.$case) {
      case "daemonOnline": {
        if (object.payload?.daemonOnline !== undefined && object.payload?.daemonOnline !== null) {
          message.payload = {
            $case: "daemonOnline",
            daemonOnline: DaemonOnline.fromPartial(object.payload.daemonOnline),
          };
        }
        break;
      }
      case "daemonOffline": {
        if (object.payload?.daemonOffline !== undefined && object.payload?.daemonOffline !== null) {
          message.payload = {
            $case: "daemonOffline",
            daemonOffline: DaemonOffline.fromPartial(object.payload.daemonOffline),
          };
        }
        break;
      }
      case "scanStarted": {
        if (object.payload?.scanStarted !== undefined && object.payload?.scanStarted !== null) {
          message.payload = { $case: "scanStarted", scanStarted: ScanStarted.fromPartial(object.payload.scanStarted) };
        }
        break;
      }
      case "scanCompleted": {
        if (object.payload?.scanCompleted !== undefined && object.payload?.scanCompleted !== null) {
          message.payload = {
            $case: "scanCompleted",
            scanCompleted: ScanCompleted.fromPartial(object.payload.scanCompleted),
          };
        }
        break;
      }
      case "gameDetected": {
        if (object.payload?.gameDetected !== undefined && object.payload?.gameDetected !== null) {
          message.payload = {
            $case: "gameDetected",
            gameDetected: GameDetected.fromPartial(object.payload.gameDetected),
          };
        }
        break;
      }
      case "gameNotFound": {
        if (object.payload?.gameNotFound !== undefined && object.payload?.gameNotFound !== null) {
          message.payload = {
            $case: "gameNotFound",
            gameNotFound: GameNotFound.fromPartial(object.payload.gameNotFound),
          };
        }
        break;
      }
      case "watching": {
        if (object.payload?.watching !== undefined && object.payload?.watching !== null) {
          message.payload = { $case: "watching", watching: Watching.fromPartial(object.payload.watching) };
        }
        break;
      }
      case "gamesDiscovered": {
        if (object.payload?.gamesDiscovered !== undefined && object.payload?.gamesDiscovered !== null) {
          message.payload = {
            $case: "gamesDiscovered",
            gamesDiscovered: GamesDiscovered.fromPartial(object.payload.gamesDiscovered),
          };
        }
        break;
      }
      case "parseStarted": {
        if (object.payload?.parseStarted !== undefined && object.payload?.parseStarted !== null) {
          message.payload = {
            $case: "parseStarted",
            parseStarted: ParseStarted.fromPartial(object.payload.parseStarted),
          };
        }
        break;
      }
      case "pluginStatus": {
        if (object.payload?.pluginStatus !== undefined && object.payload?.pluginStatus !== null) {
          message.payload = {
            $case: "pluginStatus",
            pluginStatus: PluginStatus.fromPartial(object.payload.pluginStatus),
          };
        }
        break;
      }
      case "parseCompleted": {
        if (object.payload?.parseCompleted !== undefined && object.payload?.parseCompleted !== null) {
          message.payload = {
            $case: "parseCompleted",
            parseCompleted: ParseCompleted.fromPartial(object.payload.parseCompleted),
          };
        }
        break;
      }
      case "parseFailed": {
        if (object.payload?.parseFailed !== undefined && object.payload?.parseFailed !== null) {
          message.payload = { $case: "parseFailed", parseFailed: ParseFailed.fromPartial(object.payload.parseFailed) };
        }
        break;
      }
      case "pushStarted": {
        if (object.payload?.pushStarted !== undefined && object.payload?.pushStarted !== null) {
          message.payload = { $case: "pushStarted", pushStarted: PushStarted.fromPartial(object.payload.pushStarted) };
        }
        break;
      }
      case "pushCompleted": {
        if (object.payload?.pushCompleted !== undefined && object.payload?.pushCompleted !== null) {
          message.payload = {
            $case: "pushCompleted",
            pushCompleted: PushCompleted.fromPartial(object.payload.pushCompleted),
          };
        }
        break;
      }
      case "pushFailed": {
        if (object.payload?.pushFailed !== undefined && object.payload?.pushFailed !== null) {
          message.payload = { $case: "pushFailed", pushFailed: PushFailed.fromPartial(object.payload.pushFailed) };
        }
        break;
      }
      case "pluginUpdated": {
        if (object.payload?.pluginUpdated !== undefined && object.payload?.pluginUpdated !== null) {
          message.payload = {
            $case: "pluginUpdated",
            pluginUpdated: PluginUpdated.fromPartial(object.payload.pluginUpdated),
          };
        }
        break;
      }
      case "configUpdate": {
        if (object.payload?.configUpdate !== undefined && object.payload?.configUpdate !== null) {
          message.payload = {
            $case: "configUpdate",
            configUpdate: ConfigUpdate.fromPartial(object.payload.configUpdate),
          };
        }
        break;
      }
      case "rescanGame": {
        if (object.payload?.rescanGame !== undefined && object.payload?.rescanGame !== null) {
          message.payload = { $case: "rescanGame", rescanGame: RescanGame.fromPartial(object.payload.rescanGame) };
        }
        break;
      }
      case "pluginAvailable": {
        if (object.payload?.pluginAvailable !== undefined && object.payload?.pluginAvailable !== null) {
          message.payload = {
            $case: "pluginAvailable",
            pluginAvailable: PluginAvailable.fromPartial(object.payload.pluginAvailable),
          };
        }
        break;
      }
      case "discoverGames": {
        if (object.payload?.discoverGames !== undefined && object.payload?.discoverGames !== null) {
          message.payload = {
            $case: "discoverGames",
            discoverGames: DiscoverGames.fromPartial(object.payload.discoverGames),
          };
        }
        break;
      }
      case "deviceState": {
        if (object.payload?.deviceState !== undefined && object.payload?.deviceState !== null) {
          message.payload = { $case: "deviceState", deviceState: DeviceState.fromPartial(object.payload.deviceState) };
        }
        break;
      }
      case "testPath": {
        if (object.payload?.testPath !== undefined && object.payload?.testPath !== null) {
          message.payload = { $case: "testPath", testPath: TestPath.fromPartial(object.payload.testPath) };
        }
        break;
      }
      case "testPathResult": {
        if (object.payload?.testPathResult !== undefined && object.payload?.testPathResult !== null) {
          message.payload = {
            $case: "testPathResult",
            testPathResult: TestPathResult.fromPartial(object.payload.testPathResult),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseDaemonOnline(): DaemonOnline {
  return { deviceId: "", version: "", timestamp: undefined };
}

export const DaemonOnline: MessageFns<DaemonOnline> = {
  encode(message: DaemonOnline, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DaemonOnline {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDaemonOnline();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DaemonOnline {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
    };
  },

  toJSON(message: DaemonOnline): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DaemonOnline>, I>>(base?: I): DaemonOnline {
    return DaemonOnline.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DaemonOnline>, I>>(object: I): DaemonOnline {
    const message = createBaseDaemonOnline();
    message.deviceId = object.deviceId ?? "";
    message.version = object.version ?? "";
    message.timestamp = object.timestamp ?? undefined;
    return message;
  },
};

function createBaseDaemonOffline(): DaemonOffline {
  return { deviceId: "", timestamp: undefined };
}

export const DaemonOffline: MessageFns<DaemonOffline> = {
  encode(message: DaemonOffline, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DaemonOffline {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDaemonOffline();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DaemonOffline {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
    };
  },

  toJSON(message: DaemonOffline): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DaemonOffline>, I>>(base?: I): DaemonOffline {
    return DaemonOffline.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DaemonOffline>, I>>(object: I): DaemonOffline {
    const message = createBaseDaemonOffline();
    message.deviceId = object.deviceId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    return message;
  },
};

function createBaseScanStarted(): ScanStarted {
  return { gameId: "", path: "" };
}

export const ScanStarted: MessageFns<ScanStarted> = {
  encode(message: ScanStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanStarted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: ScanStarted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScanStarted>, I>>(base?: I): ScanStarted {
    return ScanStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScanStarted>, I>>(object: I): ScanStarted {
    const message = createBaseScanStarted();
    message.gameId = object.gameId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseScanCompleted(): ScanCompleted {
  return { gameId: "", path: "", filesFound: 0, fileNames: [] };
}

export const ScanCompleted: MessageFns<ScanCompleted> = {
  encode(message: ScanCompleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.filesFound !== 0) {
      writer.uint32(24).int32(message.filesFound);
    }
    for (const v of message.fileNames) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanCompleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanCompleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.filesFound = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fileNames.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanCompleted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      filesFound: isSet(object.filesFound)
        ? globalThis.Number(object.filesFound)
        : isSet(object.files_found)
        ? globalThis.Number(object.files_found)
        : 0,
      fileNames: globalThis.Array.isArray(object?.fileNames)
        ? object.fileNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.file_names)
        ? object.file_names.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ScanCompleted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.filesFound !== 0) {
      obj.filesFound = Math.round(message.filesFound);
    }
    if (message.fileNames?.length) {
      obj.fileNames = message.fileNames;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScanCompleted>, I>>(base?: I): ScanCompleted {
    return ScanCompleted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScanCompleted>, I>>(object: I): ScanCompleted {
    const message = createBaseScanCompleted();
    message.gameId = object.gameId ?? "";
    message.path = object.path ?? "";
    message.filesFound = object.filesFound ?? 0;
    message.fileNames = object.fileNames?.map((e) => e) || [];
    return message;
  },
};

function createBaseGameDetected(): GameDetected {
  return { gameId: "", path: "", saveCount: 0 };
}

export const GameDetected: MessageFns<GameDetected> = {
  encode(message: GameDetected, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.saveCount !== 0) {
      writer.uint32(24).int32(message.saveCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameDetected {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameDetected();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.saveCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameDetected {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      saveCount: isSet(object.saveCount)
        ? globalThis.Number(object.saveCount)
        : isSet(object.save_count)
        ? globalThis.Number(object.save_count)
        : 0,
    };
  },

  toJSON(message: GameDetected): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.saveCount !== 0) {
      obj.saveCount = Math.round(message.saveCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameDetected>, I>>(base?: I): GameDetected {
    return GameDetected.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameDetected>, I>>(object: I): GameDetected {
    const message = createBaseGameDetected();
    message.gameId = object.gameId ?? "";
    message.path = object.path ?? "";
    message.saveCount = object.saveCount ?? 0;
    return message;
  },
};

function createBaseGameNotFound(): GameNotFound {
  return { gameId: "", pathsChecked: [] };
}

export const GameNotFound: MessageFns<GameNotFound> = {
  encode(message: GameNotFound, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    for (const v of message.pathsChecked) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameNotFound {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameNotFound();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pathsChecked.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameNotFound {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      pathsChecked: globalThis.Array.isArray(object?.pathsChecked)
        ? object.pathsChecked.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.paths_checked)
        ? object.paths_checked.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GameNotFound): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.pathsChecked?.length) {
      obj.pathsChecked = message.pathsChecked;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameNotFound>, I>>(base?: I): GameNotFound {
    return GameNotFound.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameNotFound>, I>>(object: I): GameNotFound {
    const message = createBaseGameNotFound();
    message.gameId = object.gameId ?? "";
    message.pathsChecked = object.pathsChecked?.map((e) => e) || [];
    return message;
  },
};

function createBaseWatching(): Watching {
  return { gameId: "", path: "", filesMonitored: 0 };
}

export const Watching: MessageFns<Watching> = {
  encode(message: Watching, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.filesMonitored !== 0) {
      writer.uint32(24).int32(message.filesMonitored);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Watching {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatching();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.filesMonitored = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Watching {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      filesMonitored: isSet(object.filesMonitored)
        ? globalThis.Number(object.filesMonitored)
        : isSet(object.files_monitored)
        ? globalThis.Number(object.files_monitored)
        : 0,
    };
  },

  toJSON(message: Watching): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.filesMonitored !== 0) {
      obj.filesMonitored = Math.round(message.filesMonitored);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Watching>, I>>(base?: I): Watching {
    return Watching.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Watching>, I>>(object: I): Watching {
    const message = createBaseWatching();
    message.gameId = object.gameId ?? "";
    message.path = object.path ?? "";
    message.filesMonitored = object.filesMonitored ?? 0;
    return message;
  },
};

function createBaseGamesDiscovered(): GamesDiscovered {
  return { games: [] };
}

export const GamesDiscovered: MessageFns<GamesDiscovered> = {
  encode(message: GamesDiscovered, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.games) {
      DiscoveredGame.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GamesDiscovered {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGamesDiscovered();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.games.push(DiscoveredGame.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GamesDiscovered {
    return {
      games: globalThis.Array.isArray(object?.games) ? object.games.map((e: any) => DiscoveredGame.fromJSON(e)) : [],
    };
  },

  toJSON(message: GamesDiscovered): unknown {
    const obj: any = {};
    if (message.games?.length) {
      obj.games = message.games.map((e) => DiscoveredGame.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GamesDiscovered>, I>>(base?: I): GamesDiscovered {
    return GamesDiscovered.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GamesDiscovered>, I>>(object: I): GamesDiscovered {
    const message = createBaseGamesDiscovered();
    message.games = object.games?.map((e) => DiscoveredGame.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDiscoveredGame(): DiscoveredGame {
  return { gameId: "", name: "", path: "", fileCount: 0 };
}

export const DiscoveredGame: MessageFns<DiscoveredGame> = {
  encode(message: DiscoveredGame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.path !== "") {
      writer.uint32(26).string(message.path);
    }
    if (message.fileCount !== 0) {
      writer.uint32(32).int32(message.fileCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoveredGame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoveredGame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fileCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscoveredGame {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      fileCount: isSet(object.fileCount)
        ? globalThis.Number(object.fileCount)
        : isSet(object.file_count)
        ? globalThis.Number(object.file_count)
        : 0,
    };
  },

  toJSON(message: DiscoveredGame): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.fileCount !== 0) {
      obj.fileCount = Math.round(message.fileCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoveredGame>, I>>(base?: I): DiscoveredGame {
    return DiscoveredGame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoveredGame>, I>>(object: I): DiscoveredGame {
    const message = createBaseDiscoveredGame();
    message.gameId = object.gameId ?? "";
    message.name = object.name ?? "";
    message.path = object.path ?? "";
    message.fileCount = object.fileCount ?? 0;
    return message;
  },
};

function createBaseParseStarted(): ParseStarted {
  return { gameId: "", fileName: "" };
}

export const ParseStarted: MessageFns<ParseStarted> = {
  encode(message: ParseStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.fileName !== "") {
      writer.uint32(18).string(message.fileName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParseStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParseStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParseStarted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
    };
  },

  toJSON(message: ParseStarted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParseStarted>, I>>(base?: I): ParseStarted {
    return ParseStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParseStarted>, I>>(object: I): ParseStarted {
    const message = createBaseParseStarted();
    message.gameId = object.gameId ?? "";
    message.fileName = object.fileName ?? "";
    return message;
  },
};

function createBasePluginStatus(): PluginStatus {
  return { gameId: "", fileName: "", message: "" };
}

export const PluginStatus: MessageFns<PluginStatus> = {
  encode(message: PluginStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.fileName !== "") {
      writer.uint32(18).string(message.fileName);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PluginStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePluginStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PluginStatus {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: PluginStatus): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PluginStatus>, I>>(base?: I): PluginStatus {
    return PluginStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PluginStatus>, I>>(object: I): PluginStatus {
    const message = createBasePluginStatus();
    message.gameId = object.gameId ?? "";
    message.fileName = object.fileName ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseParseCompleted(): ParseCompleted {
  return { gameId: "", fileName: "", identity: undefined, summary: "", sectionsCount: 0, sizeBytes: 0 };
}

export const ParseCompleted: MessageFns<ParseCompleted> = {
  encode(message: ParseCompleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.fileName !== "") {
      writer.uint32(18).string(message.fileName);
    }
    if (message.identity !== undefined) {
      SaveIdentity.encode(message.identity, writer.uint32(26).fork()).join();
    }
    if (message.summary !== "") {
      writer.uint32(34).string(message.summary);
    }
    if (message.sectionsCount !== 0) {
      writer.uint32(40).int32(message.sectionsCount);
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(48).int64(message.sizeBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParseCompleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParseCompleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.identity = SaveIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sectionsCount = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sizeBytes = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParseCompleted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      identity: isSet(object.identity) ? SaveIdentity.fromJSON(object.identity) : undefined,
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      sectionsCount: isSet(object.sectionsCount)
        ? globalThis.Number(object.sectionsCount)
        : isSet(object.sections_count)
        ? globalThis.Number(object.sections_count)
        : 0,
      sizeBytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
    };
  },

  toJSON(message: ParseCompleted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.identity !== undefined) {
      obj.identity = SaveIdentity.toJSON(message.identity);
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.sectionsCount !== 0) {
      obj.sectionsCount = Math.round(message.sectionsCount);
    }
    if (message.sizeBytes !== 0) {
      obj.sizeBytes = Math.round(message.sizeBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParseCompleted>, I>>(base?: I): ParseCompleted {
    return ParseCompleted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParseCompleted>, I>>(object: I): ParseCompleted {
    const message = createBaseParseCompleted();
    message.gameId = object.gameId ?? "";
    message.fileName = object.fileName ?? "";
    message.identity = (object.identity !== undefined && object.identity !== null)
      ? SaveIdentity.fromPartial(object.identity)
      : undefined;
    message.summary = object.summary ?? "";
    message.sectionsCount = object.sectionsCount ?? 0;
    message.sizeBytes = object.sizeBytes ?? 0;
    return message;
  },
};

function createBaseParseFailed(): ParseFailed {
  return { gameId: "", fileName: "", errorType: 0, message: "" };
}

export const ParseFailed: MessageFns<ParseFailed> = {
  encode(message: ParseFailed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.fileName !== "") {
      writer.uint32(18).string(message.fileName);
    }
    if (message.errorType !== 0) {
      writer.uint32(24).int32(message.errorType);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParseFailed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParseFailed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.errorType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParseFailed {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      errorType: isSet(object.errorType)
        ? parseErrorTypeFromJSON(object.errorType)
        : isSet(object.error_type)
        ? parseErrorTypeFromJSON(object.error_type)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: ParseFailed): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.errorType !== 0) {
      obj.errorType = parseErrorTypeToJSON(message.errorType);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParseFailed>, I>>(base?: I): ParseFailed {
    return ParseFailed.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParseFailed>, I>>(object: I): ParseFailed {
    const message = createBaseParseFailed();
    message.gameId = object.gameId ?? "";
    message.fileName = object.fileName ?? "";
    message.errorType = object.errorType ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBasePushStarted(): PushStarted {
  return { gameId: "", summary: "", sizeBytes: 0 };
}

export const PushStarted: MessageFns<PushStarted> = {
  encode(message: PushStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.summary !== "") {
      writer.uint32(18).string(message.summary);
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(24).int64(message.sizeBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sizeBytes = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushStarted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      sizeBytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
    };
  },

  toJSON(message: PushStarted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.sizeBytes !== 0) {
      obj.sizeBytes = Math.round(message.sizeBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushStarted>, I>>(base?: I): PushStarted {
    return PushStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushStarted>, I>>(object: I): PushStarted {
    const message = createBasePushStarted();
    message.gameId = object.gameId ?? "";
    message.summary = object.summary ?? "";
    message.sizeBytes = object.sizeBytes ?? 0;
    return message;
  },
};

function createBasePushCompleted(): PushCompleted {
  return { gameId: "", saveUuid: "", summary: "", snapshotSizeBytes: 0, durationMs: 0 };
}

export const PushCompleted: MessageFns<PushCompleted> = {
  encode(message: PushCompleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.saveUuid !== "") {
      writer.uint32(18).string(message.saveUuid);
    }
    if (message.summary !== "") {
      writer.uint32(26).string(message.summary);
    }
    if (message.snapshotSizeBytes !== 0) {
      writer.uint32(32).int64(message.snapshotSizeBytes);
    }
    if (message.durationMs !== 0) {
      writer.uint32(40).int32(message.durationMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushCompleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushCompleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.saveUuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.snapshotSizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.durationMs = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushCompleted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      saveUuid: isSet(object.saveUuid)
        ? globalThis.String(object.saveUuid)
        : isSet(object.save_uuid)
        ? globalThis.String(object.save_uuid)
        : "",
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      snapshotSizeBytes: isSet(object.snapshotSizeBytes)
        ? globalThis.Number(object.snapshotSizeBytes)
        : isSet(object.snapshot_size_bytes)
        ? globalThis.Number(object.snapshot_size_bytes)
        : 0,
      durationMs: isSet(object.durationMs)
        ? globalThis.Number(object.durationMs)
        : isSet(object.duration_ms)
        ? globalThis.Number(object.duration_ms)
        : 0,
    };
  },

  toJSON(message: PushCompleted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.saveUuid !== "") {
      obj.saveUuid = message.saveUuid;
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.snapshotSizeBytes !== 0) {
      obj.snapshotSizeBytes = Math.round(message.snapshotSizeBytes);
    }
    if (message.durationMs !== 0) {
      obj.durationMs = Math.round(message.durationMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushCompleted>, I>>(base?: I): PushCompleted {
    return PushCompleted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushCompleted>, I>>(object: I): PushCompleted {
    const message = createBasePushCompleted();
    message.gameId = object.gameId ?? "";
    message.saveUuid = object.saveUuid ?? "";
    message.summary = object.summary ?? "";
    message.snapshotSizeBytes = object.snapshotSizeBytes ?? 0;
    message.durationMs = object.durationMs ?? 0;
    return message;
  },
};

function createBasePushFailed(): PushFailed {
  return { gameId: "", message: "", willRetry: false };
}

export const PushFailed: MessageFns<PushFailed> = {
  encode(message: PushFailed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.willRetry !== false) {
      writer.uint32(24).bool(message.willRetry);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushFailed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushFailed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.willRetry = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushFailed {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      willRetry: isSet(object.willRetry)
        ? globalThis.Boolean(object.willRetry)
        : isSet(object.will_retry)
        ? globalThis.Boolean(object.will_retry)
        : false,
    };
  },

  toJSON(message: PushFailed): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.willRetry !== false) {
      obj.willRetry = message.willRetry;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushFailed>, I>>(base?: I): PushFailed {
    return PushFailed.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushFailed>, I>>(object: I): PushFailed {
    const message = createBasePushFailed();
    message.gameId = object.gameId ?? "";
    message.message = object.message ?? "";
    message.willRetry = object.willRetry ?? false;
    return message;
  },
};

function createBasePluginUpdated(): PluginUpdated {
  return { gameId: "", version: "" };
}

export const PluginUpdated: MessageFns<PluginUpdated> = {
  encode(message: PluginUpdated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PluginUpdated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePluginUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PluginUpdated {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: PluginUpdated): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PluginUpdated>, I>>(base?: I): PluginUpdated {
    return PluginUpdated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PluginUpdated>, I>>(object: I): PluginUpdated {
    const message = createBasePluginUpdated();
    message.gameId = object.gameId ?? "";
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseConfigUpdate(): ConfigUpdate {
  return { games: {} };
}

export const ConfigUpdate: MessageFns<ConfigUpdate> = {
  encode(message: ConfigUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.games).forEach(([key, value]: [string, GameConfig]) => {
      ConfigUpdate_GamesEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = ConfigUpdate_GamesEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.games[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigUpdate {
    return {
      games: isObject(object.games)
        ? (globalThis.Object.entries(object.games) as [string, any][]).reduce(
          (acc: { [key: string]: GameConfig }, [key, value]: [string, any]) => {
            acc[key] = GameConfig.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ConfigUpdate): unknown {
    const obj: any = {};
    if (message.games) {
      const entries = globalThis.Object.entries(message.games) as [string, GameConfig][];
      if (entries.length > 0) {
        obj.games = {};
        entries.forEach(([k, v]) => {
          obj.games[k] = GameConfig.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigUpdate>, I>>(base?: I): ConfigUpdate {
    return ConfigUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigUpdate>, I>>(object: I): ConfigUpdate {
    const message = createBaseConfigUpdate();
    message.games = (globalThis.Object.entries(object.games ?? {}) as [string, GameConfig][]).reduce(
      (acc: { [key: string]: GameConfig }, [key, value]: [string, GameConfig]) => {
        if (value !== undefined) {
          acc[key] = GameConfig.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseConfigUpdate_GamesEntry(): ConfigUpdate_GamesEntry {
  return { key: "", value: undefined };
}

export const ConfigUpdate_GamesEntry: MessageFns<ConfigUpdate_GamesEntry> = {
  encode(message: ConfigUpdate_GamesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      GameConfig.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigUpdate_GamesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigUpdate_GamesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = GameConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigUpdate_GamesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? GameConfig.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ConfigUpdate_GamesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = GameConfig.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigUpdate_GamesEntry>, I>>(base?: I): ConfigUpdate_GamesEntry {
    return ConfigUpdate_GamesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigUpdate_GamesEntry>, I>>(object: I): ConfigUpdate_GamesEntry {
    const message = createBaseConfigUpdate_GamesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? GameConfig.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseGameConfig(): GameConfig {
  return { savePath: "", enabled: false, fileExtensions: [] };
}

export const GameConfig: MessageFns<GameConfig> = {
  encode(message: GameConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.savePath !== "") {
      writer.uint32(10).string(message.savePath);
    }
    if (message.enabled !== false) {
      writer.uint32(16).bool(message.enabled);
    }
    for (const v of message.fileExtensions) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.savePath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileExtensions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameConfig {
    return {
      savePath: isSet(object.savePath)
        ? globalThis.String(object.savePath)
        : isSet(object.save_path)
        ? globalThis.String(object.save_path)
        : "",
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      fileExtensions: globalThis.Array.isArray(object?.fileExtensions)
        ? object.fileExtensions.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.file_extensions)
        ? object.file_extensions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GameConfig): unknown {
    const obj: any = {};
    if (message.savePath !== "") {
      obj.savePath = message.savePath;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.fileExtensions?.length) {
      obj.fileExtensions = message.fileExtensions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameConfig>, I>>(base?: I): GameConfig {
    return GameConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameConfig>, I>>(object: I): GameConfig {
    const message = createBaseGameConfig();
    message.savePath = object.savePath ?? "";
    message.enabled = object.enabled ?? false;
    message.fileExtensions = object.fileExtensions?.map((e) => e) || [];
    return message;
  },
};

function createBaseRescanGame(): RescanGame {
  return { gameId: "" };
}

export const RescanGame: MessageFns<RescanGame> = {
  encode(message: RescanGame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RescanGame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRescanGame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RescanGame {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
    };
  },

  toJSON(message: RescanGame): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RescanGame>, I>>(base?: I): RescanGame {
    return RescanGame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RescanGame>, I>>(object: I): RescanGame {
    const message = createBaseRescanGame();
    message.gameId = object.gameId ?? "";
    return message;
  },
};

function createBasePluginAvailable(): PluginAvailable {
  return { gameId: "", version: "", url: "" };
}

export const PluginAvailable: MessageFns<PluginAvailable> = {
  encode(message: PluginAvailable, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PluginAvailable {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePluginAvailable();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PluginAvailable {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: PluginAvailable): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PluginAvailable>, I>>(base?: I): PluginAvailable {
    return PluginAvailable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PluginAvailable>, I>>(object: I): PluginAvailable {
    const message = createBasePluginAvailable();
    message.gameId = object.gameId ?? "";
    message.version = object.version ?? "";
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseDiscoverGames(): DiscoverGames {
  return {};
}

export const DiscoverGames: MessageFns<DiscoverGames> = {
  encode(_: DiscoverGames, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverGames {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverGames();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DiscoverGames {
    return {};
  },

  toJSON(_: DiscoverGames): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoverGames>, I>>(base?: I): DiscoverGames {
    return DiscoverGames.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoverGames>, I>>(_: I): DiscoverGames {
    const message = createBaseDiscoverGames();
    return message;
  },
};

function createBaseDeviceState(): DeviceState {
  return { devices: [] };
}

export const DeviceState: MessageFns<DeviceState> = {
  encode(message: DeviceState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.devices) {
      DeviceInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.devices.push(DeviceInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceState {
    return {
      devices: globalThis.Array.isArray(object?.devices) ? object.devices.map((e: any) => DeviceInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: DeviceState): unknown {
    const obj: any = {};
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => DeviceInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceState>, I>>(base?: I): DeviceState {
    return DeviceState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceState>, I>>(object: I): DeviceState {
    const message = createBaseDeviceState();
    message.devices = object.devices?.map((e) => DeviceInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeviceInfo(): DeviceInfo {
  return { deviceId: "", online: false, lastSeen: undefined, games: [] };
}

export const DeviceInfo: MessageFns<DeviceInfo> = {
  encode(message: DeviceInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.online !== false) {
      writer.uint32(16).bool(message.online);
    }
    if (message.lastSeen !== undefined) {
      Timestamp.encode(toTimestamp(message.lastSeen), writer.uint32(26).fork()).join();
    }
    for (const v of message.games) {
      GameInfo.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.online = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastSeen = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.games.push(GameInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceInfo {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
      lastSeen: isSet(object.lastSeen)
        ? fromJsonTimestamp(object.lastSeen)
        : isSet(object.last_seen)
        ? fromJsonTimestamp(object.last_seen)
        : undefined,
      games: globalThis.Array.isArray(object?.games) ? object.games.map((e: any) => GameInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: DeviceInfo): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.online !== false) {
      obj.online = message.online;
    }
    if (message.lastSeen !== undefined) {
      obj.lastSeen = message.lastSeen.toISOString();
    }
    if (message.games?.length) {
      obj.games = message.games.map((e) => GameInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceInfo>, I>>(base?: I): DeviceInfo {
    return DeviceInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceInfo>, I>>(object: I): DeviceInfo {
    const message = createBaseDeviceInfo();
    message.deviceId = object.deviceId ?? "";
    message.online = object.online ?? false;
    message.lastSeen = object.lastSeen ?? undefined;
    message.games = object.games?.map((e) => GameInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGameInfo(): GameInfo {
  return { gameId: "", gameName: "", status: 0, saves: [], lastActivity: undefined };
}

export const GameInfo: MessageFns<GameInfo> = {
  encode(message: GameInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.gameName !== "") {
      writer.uint32(18).string(message.gameName);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    for (const v of message.saves) {
      SaveInfo.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.lastActivity !== undefined) {
      Timestamp.encode(toTimestamp(message.lastActivity), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gameName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.saves.push(SaveInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastActivity = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameInfo {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      gameName: isSet(object.gameName)
        ? globalThis.String(object.gameName)
        : isSet(object.game_name)
        ? globalThis.String(object.game_name)
        : "",
      status: isSet(object.status) ? gameStatusEnumFromJSON(object.status) : 0,
      saves: globalThis.Array.isArray(object?.saves) ? object.saves.map((e: any) => SaveInfo.fromJSON(e)) : [],
      lastActivity: isSet(object.lastActivity)
        ? fromJsonTimestamp(object.lastActivity)
        : isSet(object.last_activity)
        ? fromJsonTimestamp(object.last_activity)
        : undefined,
    };
  },

  toJSON(message: GameInfo): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.gameName !== "") {
      obj.gameName = message.gameName;
    }
    if (message.status !== 0) {
      obj.status = gameStatusEnumToJSON(message.status);
    }
    if (message.saves?.length) {
      obj.saves = message.saves.map((e) => SaveInfo.toJSON(e));
    }
    if (message.lastActivity !== undefined) {
      obj.lastActivity = message.lastActivity.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameInfo>, I>>(base?: I): GameInfo {
    return GameInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameInfo>, I>>(object: I): GameInfo {
    const message = createBaseGameInfo();
    message.gameId = object.gameId ?? "";
    message.gameName = object.gameName ?? "";
    message.status = object.status ?? 0;
    message.saves = object.saves?.map((e) => SaveInfo.fromPartial(e)) || [];
    message.lastActivity = object.lastActivity ?? undefined;
    return message;
  },
};

function createBaseSaveInfo(): SaveInfo {
  return { saveUuid: "", identity: undefined, summary: "", lastUpdated: undefined };
}

export const SaveInfo: MessageFns<SaveInfo> = {
  encode(message: SaveInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.saveUuid !== "") {
      writer.uint32(10).string(message.saveUuid);
    }
    if (message.identity !== undefined) {
      SaveIdentity.encode(message.identity, writer.uint32(18).fork()).join();
    }
    if (message.summary !== "") {
      writer.uint32(26).string(message.summary);
    }
    if (message.lastUpdated !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdated), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.saveUuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.identity = SaveIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastUpdated = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaveInfo {
    return {
      saveUuid: isSet(object.saveUuid)
        ? globalThis.String(object.saveUuid)
        : isSet(object.save_uuid)
        ? globalThis.String(object.save_uuid)
        : "",
      identity: isSet(object.identity) ? SaveIdentity.fromJSON(object.identity) : undefined,
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      lastUpdated: isSet(object.lastUpdated)
        ? fromJsonTimestamp(object.lastUpdated)
        : isSet(object.last_updated)
        ? fromJsonTimestamp(object.last_updated)
        : undefined,
    };
  },

  toJSON(message: SaveInfo): unknown {
    const obj: any = {};
    if (message.saveUuid !== "") {
      obj.saveUuid = message.saveUuid;
    }
    if (message.identity !== undefined) {
      obj.identity = SaveIdentity.toJSON(message.identity);
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.lastUpdated !== undefined) {
      obj.lastUpdated = message.lastUpdated.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SaveInfo>, I>>(base?: I): SaveInfo {
    return SaveInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SaveInfo>, I>>(object: I): SaveInfo {
    const message = createBaseSaveInfo();
    message.saveUuid = object.saveUuid ?? "";
    message.identity = (object.identity !== undefined && object.identity !== null)
      ? SaveIdentity.fromPartial(object.identity)
      : undefined;
    message.summary = object.summary ?? "";
    message.lastUpdated = object.lastUpdated ?? undefined;
    return message;
  },
};

function createBaseTestPath(): TestPath {
  return { gameId: "", path: "" };
}

export const TestPath: MessageFns<TestPath> = {
  encode(message: TestPath, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestPath {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestPath();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestPath {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: TestPath): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TestPath>, I>>(base?: I): TestPath {
    return TestPath.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestPath>, I>>(object: I): TestPath {
    const message = createBaseTestPath();
    message.gameId = object.gameId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseTestPathResult(): TestPathResult {
  return { gameId: "", path: "", valid: false, filesFound: 0, fileNames: [] };
}

export const TestPathResult: MessageFns<TestPathResult> = {
  encode(message: TestPathResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.valid !== false) {
      writer.uint32(24).bool(message.valid);
    }
    if (message.filesFound !== 0) {
      writer.uint32(32).int32(message.filesFound);
    }
    for (const v of message.fileNames) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestPathResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestPathResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.filesFound = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fileNames.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestPathResult {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
      filesFound: isSet(object.filesFound)
        ? globalThis.Number(object.filesFound)
        : isSet(object.files_found)
        ? globalThis.Number(object.files_found)
        : 0,
      fileNames: globalThis.Array.isArray(object?.fileNames)
        ? object.fileNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.file_names)
        ? object.file_names.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: TestPathResult): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    if (message.filesFound !== 0) {
      obj.filesFound = Math.round(message.filesFound);
    }
    if (message.fileNames?.length) {
      obj.fileNames = message.fileNames;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TestPathResult>, I>>(base?: I): TestPathResult {
    return TestPathResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestPathResult>, I>>(object: I): TestPathResult {
    const message = createBaseTestPathResult();
    message.gameId = object.gameId ?? "";
    message.path = object.path ?? "";
    message.valid = object.valid ?? false;
    message.filesFound = object.filesFound ?? 0;
    message.fileNames = object.fileNames?.map((e) => e) || [];
    return message;
  },
};

function createBaseSaveIdentity(): SaveIdentity {
  return { name: "", extra: undefined };
}

export const SaveIdentity: MessageFns<SaveIdentity> = {
  encode(message: SaveIdentity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.extra !== undefined) {
      Struct.encode(Struct.wrap(message.extra), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveIdentity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveIdentity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.extra = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaveIdentity {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      extra: isObject(object.extra) ? object.extra : undefined,
    };
  },

  toJSON(message: SaveIdentity): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.extra !== undefined) {
      obj.extra = message.extra;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SaveIdentity>, I>>(base?: I): SaveIdentity {
    return SaveIdentity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SaveIdentity>, I>>(object: I): SaveIdentity {
    const message = createBaseSaveIdentity();
    message.name = object.name ?? "";
    message.extra = object.extra ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
